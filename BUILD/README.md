# BUILD

Diretório de saída intermediária gerado localmente pela pipeline.

`xml2ors_fixed_v4_ptbr_folder.py` cria:

```text
BUILD\Script\
```

Essa estrutura é usada pelo repacker para gerar `OUTPUT\Script.GPK`.

Os arquivos desta pasta são gerados e não devem ser versionados.